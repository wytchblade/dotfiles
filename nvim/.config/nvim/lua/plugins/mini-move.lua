return {
  'nvim-mini/mini.nvim', version = false,
  opts = {
    -- add default mappings
    mappings = true,
    -- add default motions
    motions = true,
    -- add default operators
    operators = true,
    -- add default text objects
    text_objects = true,
  },
}
